{ inputs, ... }: {
  # This allow for unstable packages to be accessed by pkgs.unstable.<your-package>
  unstable-packages = final: _prev: {
    unstable = import inputs.nixpkgs-unstable
      {
        system = final.system;
        config.allowUnfree = true;
      };
  };
}
